CREATE TABLE CREW(
    ID NUMBER PRIMARY KEY,
    NAME VARCHAR2(20) NOT NULL UNIQUE,
    MASTER VARCHAR2(20) NOT NULL
);

INSERT INTO CREW VALUES(2, 'DOORUN', 'DOORUN');

CREATE TABLE BOARD(
    ID NUMBER PRIMARY KEY, -- 0: 이벤트 / 1: 자유 / 2~: 크루(크루id 사용)
    NAME VARCHAR2(20) NOT NULL UNIQUE 
);

INSERT INTO BOARD VALUES(0, 'EVENT_BOARD');
INSERT INTO BOARD VALUES(1, 'FREE_BOARD');
INSERT INTO BOARD VALUES(2, 'DOORUN');

CREATE TABLE POST( -- 조회수 추가, 첨부파일 추가
    ID NUMBER PRIMARY KEY,
    TITLE VARCHAR2(100) NOT NULL,
    CONTENT VARCHAR2(2000),
    WRITER VARCHAR2(20) NOT NULL, -- 후에 USER 테이블 FK 추가하기
    IMAGE_FILE VARCHAR2(100),
    REG_DATE DATE DEFAULT SYSDATE NOT NULL,
    BOARD_ID NUMBER NOT NULL,
    CONSTRAINT FK_BOARD_ID FOREIGN KEY(BOARD_ID) REFERENCES BOARD(ID) ON DELETE CASCADE
);

INSERT INTO POST(ID, TITLE, CONTENT, WRITER, BOARD_ID)
VALUES(1, '안녕하세요', '안녕하세요~ 처음 뵙겠습니다', 'DOORUN', 0);

INSERT INTO POST(ID, TITLE, CONTENT, WRITER, BOARD_ID)
VALUES((SELECT NVL(MAX(ID),1) FROM POST)+1, '안녕하세요', '안녕하세요~ 처음 뵙겠습니다', 'DOORUN', 1);

INSERT INTO POST(ID, TITLE, CONTENT, WRITER, BOARD_ID)
VALUES((SELECT NVL(MAX(ID),1) FROM POST)+1, '안녕하세요', '안녕하세요~ 처음 뵙겠습니다', 'DOORUN', 2);

CREATE TABLE REPLY(
    ID NUMBER PRIMARY KEY,
    WRITER VARCHAR2(20) NOT NULL, -- 후에 USER 테이블 FK 추가하기
    CONTENT VARCHAR2(500),
    POST_ID NUMBER NOT NULL,
    REG_DATE DATE DEFAULT SYSDATE NOT NULL,
    PARENT_ID NUMBER DEFAULT 0 NOT NULL, -- 0 이면 부모 댓글 없음
    CONSTRAINT FK_POST_ID FOREIGN KEY(POST_ID) REFERENCES POST(ID) ON DELETE CASCADE
);

INSERT INTO REPLY(ID, WRITER, CONTENT, POST_ID)
VALUES((SELECT NVL(MAX(ID),0) FROM REPLY)+1, 'DOORUN', 'EVENT',1);

INSERT INTO REPLY(ID, WRITER, CONTENT, POST_ID)
VALUES((SELECT NVL(MAX(ID),0) FROM REPLY)+1, 'DOORUN', 'FREE',2);

INSERT INTO REPLY(ID, WRITER, CONTENT, POST_ID)
VALUES((SELECT NVL(MAX(ID),0) FROM REPLY)+1, 'DOORUN', 'DOORUN',3);

COMMIT;